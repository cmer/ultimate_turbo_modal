<%= turbo_stream.modal(:hide) %>
<%= turbo_stream.replace "ts-notice", partial: "testing/hide_from_backends/notice", locals: { message: "Modal close was triggered by the backend via Turbo Stream @ #{Time.now}!" } %>
