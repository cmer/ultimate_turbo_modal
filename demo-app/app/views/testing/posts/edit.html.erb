<%= modal(title: "Editing Post") do |m| %>
  <div class="mx-auto w-96">
    <% if !inside_modal? %>
      <h1 class="font-bold text-4xl dark:text-white">Editing post</h1>
    <% end %>

    <%= render "testing/posts/form", post: @post, modal: m %>
  </div>
<% end %>
